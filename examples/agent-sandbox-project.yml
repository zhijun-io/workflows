# agent-sandbox project configuration
# Place this file at .github/project.yml in the agent-sandbox repository

name: agent-sandbox

release:
  current-version: "0.1.0-SNAPSHOT"
  next-version: "0.2.0-SNAPSHOT"

integration:
  uses-reusable-workflows: true
  pr-based-releases: false
  java-version: "21"
